rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Scholarship uploads: lib/scholarship_application_helper.dart → Documents/{uuid}/{filename}
    match /Documents/{documentId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
